function Phone (name, price, screen, bCamera, fCamera, img, desc, type) {
    this.name = name;
    this.price = price;
    this.screen = screen;
    this.backCamera = bCamera;
    this.frontCamera = fCamera;
    this.img =img;
    this.desc = desc;
    this.type = type;
}